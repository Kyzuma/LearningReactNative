import { View, Text } from "react-native";

const ContactScreen = () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text>Contact!</Text>
  </View>
);

// Allow the ContactScreen component to be imported in other files
export default ContactScreen;
